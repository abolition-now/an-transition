import {Stylesheet} from "@canopy-iiif/app/head";
import {CanopyHeader} from "@canopy-iiif/app/ui";

export function Head() {
  return (
    <>
      <Stylesheet />
      <link rel="preconnect" href="https://fonts.googleapis.com" />
      <link
        rel="preconnect"
        href="https://fonts.gstatic.com"
        crossOrigin="true"
      />
      <link
        href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght,SOFT,WONK@0,9..144,300..600,50,0..1;1,9..144,300..600,50,0..1&display=swap"
        rel="stylesheet"
      />
    </>
  );
}

export function App({children}) {
  const navigation = [
    {href: '/search?type=work', label: 'Works'},
    {href: '/guides', label: 'Guides'},
    {href: '/contact', label: 'Contact'},
  ];

return (

<div>
  <CanopyHeader navigation={navigation} title="abolition now."></CanopyHeader>
  <main className="max-w-none">{children}</main>
  <footer className="border-t border-slate-200 text-slate-500">
    <div className="mx-auto max-w-content w-full px-4 py-6">(footer)</div>
  </footer>
</div>
); }
